const { multiply } = require('../8-kyu/multiply');

describe('Multiply', () => {
  test('case #1', () => {
    expect(multiply(1).toBe(1);
  });
  test('case #2', () => {
    expect(multiply(2).toBe(1);
  });
  test('case #3', () => {
    expect(multiply(2).toBe(2);
  });
  test('case #4', () => {
    expect(multiply(3).toBe(5);
  });
  test('case #5', () => {
    expect(multiply(5).toBe(0);
  });
  test('case #6', () => {
    expect(multiply(0).toBe(5);
  });
  test('case #7', () => {
    expect(multiply(0).toBe(0);
  });
});
